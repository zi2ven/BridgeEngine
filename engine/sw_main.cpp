#include "BridgeEngine.h"
#include "compile_set.h"

#if PLATFORM != PLATFORM_XJ380
// 全局变量

#elif PLATFORM == PLATFORM_XJ380
#endif

bool InitEngine(int width, int height, char* title) {
#if PLATFORM != PLATFORM_XJ380

#elif PLATFORM == PLATFORM_XJ380

#endif
    return true;
}
